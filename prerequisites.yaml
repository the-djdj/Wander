---
# The bash prerequisite
preamble:
    LC_ALL: C
elements:
    bash:
        description: Bash
        version: 3.2
        commands:
            - bash --version | head -n1 | cut -d" " -f4 | cut -d"(" -f1
...
